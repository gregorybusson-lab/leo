# ℹ️ Note sur les erreurs CSS

## Erreurs affichées dans VS Code

Vous pouvez voir des erreurs dans `src/index.css` concernant :
- `@tailwind` directive inconnue
- `@apply` directive inconnue

## ✅ C'est normal !

Ces erreurs sont de **fausses alertes** de l'éditeur VS Code. Le CSS fonctionne parfaitement car :

1. **PostCSS** traite ces directives Tailwind avant compilation
2. Le serveur Vite compile correctement le CSS
3. Le site fonctionne sans problème

## 🔧 Pour supprimer ces warnings (optionnel)

Installer l'extension VS Code "Tailwind CSS IntelliSense" :

```
ID : bradlc.vscode-tailwindcss
```

Cette extension :
- ✅ Supprime les warnings
- ✅ Ajoute l'autocomplétion des classes Tailwind
- ✅ Affiche les couleurs et espacements
- ✅ Améliore l'expérience de développement

## Installation automatique

Si vous avez l'extension installée, VS Code peut aussi créer un fichier `.vscode/settings.json` :

```json
{
  "css.validate": false,
  "scss.validate": false,
  "less.validate": false,
  "tailwindCSS.emmetCompletions": true
}
```

## 🎯 Conclusion

**Vous pouvez ignorer ces erreurs** — elles n'impactent pas le fonctionnement du site.  
Le build fonctionne et le site s'affiche correctement ! ✨
